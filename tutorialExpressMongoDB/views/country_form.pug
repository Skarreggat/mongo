extends layout

block content
  h1=title

  form(method='POST' action='')
    div.form-group
      label(for='name') Name:
      input#name.form-control(type='text' placeholder='name (Españita) last' name='name' required='true' value=(undefined===country ? '' : country.name) )
      label(for='continent') Continent:
      input#continent.form-control(type='text' placeholder='continent (Europa)' name='continent' value=(undefined===country ? '' : country.continent)
    button.btn.btn-primary(type='submit') Submit
  if errors 
    ul
      for error in errors
        li!= error.msg
